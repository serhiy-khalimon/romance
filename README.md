# www.romanceabroad.com

**About**
* Java 11
* Selenium
* TestNG
* Maven

**How to run**
* Open a command prompt
* Go to the project directory
* Run a command `mvn test` or `mvn clean install`

Alternatively
* Open an IDE
* Run the `testng.xml` file